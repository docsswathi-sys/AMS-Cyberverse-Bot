[project]
name = "ams-cyberverse-api"
version = "1.0.0"
description = "AMS Cyberverse FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg[binary]==3.3.6",
]

[tool.vercel]
entrypoint = "backend.main:app"
